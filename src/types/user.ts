/**
 * User model
 * @prop {string} name User name
 * @prop {string} phone User of phone number
 * @prop {string} address User of address
 */
export default interface User {
  name: string;
  phone: string;
  address: string;
}
